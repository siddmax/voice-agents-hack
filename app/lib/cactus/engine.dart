// Lane A owns this module.
// Thin wrapper around cactus.dart. Loads Gemma 4 E4B, exposes streaming
// completion + tool calls, provides a JSON parse-and-retry helper because
// the C FFI does not expose set_tool_constraints from Dart.

// TODO(lane-a): implement. See task #2.
